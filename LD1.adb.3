with Text_IO;
use Text_IO;
procedure LD1 is


type Hundred is new Integer range 0..100;

package Hundred_IO is new Integer_IO(Hundred);
use Hundred_IO;

type Measurements is array (Character range <>, Character range <>) of Hundred;


Results : Measurements('a'..'d', 'e'..'i') := (
                );

begin
Put(Results('a','e'));
New_Line;
end LD1;